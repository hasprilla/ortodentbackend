version: '3.8'

services:
  laravel:
    image: php:8.2-cli
    container_name: ortodentbackend
    working_dir: /var/www/html
    volumes:
      - .:/var/www/html
    ports:
      - "8000:8000"
    command: php artisan serve --host=0.0.0.0 --port=8000
